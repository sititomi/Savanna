using System;

public class TurnPacer //léptető
{
	public Pacer()
	{
        Turn round = new Turn(); //kör változó, vár egy 
        do
        {
            switch (pressButton) { 
                case (Entert nyom a felhasználó):
                round += 1;

            case (Escapet noym ):
                Kilép a ciklusból
                exit;

            case (Bármi mást nyom):
                ;
            }

            } while(végtelenített ciklus);
             //bezárjuk a programot, ha már kiléptünk a ciklusból, azaz ESC
	}
}
